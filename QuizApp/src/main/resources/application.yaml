spring:
  application:
    name: QuizApp
  datasource:
    url: jdbc:mysql://localhost:3306/quizApp
    username: root
    password: Poppingsoul
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
      database: MYSQL
      open-in-view: true
server:
  error:
    include-stacktrace: always
    include-message: always
  port: 7079
